import { PageLoading } from "@/components/ui";

export default function Loading() {
  return <PageLoading variant="application" title="Preparing your application step…" message="Loading the form, progress guide, and saved application details." />;
}
