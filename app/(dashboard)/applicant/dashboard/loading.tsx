import { PageLoading } from "@/components/ui";

export default function Loading() {
  return <PageLoading title="Preparing your applicant dashboard…" message="Loading your application status, document checklist, and next step." />;
}
